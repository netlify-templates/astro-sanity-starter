import { type ModelExtension } from '@stackbit/types';
import { commonFields } from './sectionCommon';

export const ctaSection: ModelExtension = {
    name: 'ctaSection',
    fields: [...commonFields]
};
